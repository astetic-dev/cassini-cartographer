# Handover

Newest entry last. Stage 01 reads the last entry before it starts.

## 4 May - a fourth stage for recoveries

Sketched a stage that would pull the foreign rings out of the week and write one
notice each, so the warden stops doing it from the paper file. The folder and the
contract are in `stages/04-recoveries/`.

Parked the same week. The scheme takes recoveries through its own portal and
wants them within 48 hours, which is faster than a Monday run. Nothing routes to
the stage and nothing in `CONTEXT.md` mentions it. The warden still does these by
hand.

## 2 June - the scheme dropped the CSV upload

From this season the scheme takes the return through a web form instead of a CSV
upload. Stage 02 now writes a markdown table a ringer copies in by hand.

`references/scheme-columns.md` describes the CSV. It was the spec stage 02 worked
from until this changed.

The form also stopped asking for ring size. The log still has the Ring size
column because the paper file uses it; no stage reads it.

## 6 July - week 27 run

Nothing open. Nets 5 and 6 were shut on Friday for the hedge cut.

## 13 July - week 28 run

Tally checked against the footer and agrees. Two returns this week, both ringed
here in 2024.

Open: Joris asked whether the post should name visiting ringers in full. Not
decided, so week 28 uses first names for everyone.
